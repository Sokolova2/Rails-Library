%button.navbar-toggler.border-0{"aria-controls" => "offcanvasNavbar", "aria-label" => "Toggle navigation", "data-bs-target" => "#offcanvasNavbar", "data-bs-toggle" => "offcanvas", :type => "button"}
  %span.fs-5
    ☰
#offcanvasNavbar.offcanvas.offcanvas-end{"aria-labelledby" => "offcanvasNavbarLabel", :tabindex => "-1"}
  .offcanvas-body
    %ul.navbar-nav.justify-content-end.flex-grow-1.pe-3
      .d-block.ms-2
        .mb-3
          -if user_signed_in?
            .d-flex.align-items-center.gap-2.mb-2
              = image_tag current_user.avatar.url, class: 'rounded-circle avatar'
              %p= current_user.login
            %li
              %hr.solid
          -unless user_signed_in?
            = button_to 'Sign in/Register', new_user_session_path, method: :get, class: 'btn btn-dark rounded-5 w-100'
        .mb-3
          = link_to books_path, class: 'text-decoration-none text-dark rounded-circle' do
            .d-flex.align-items-center.gap-3
              %i.bi.bi-ui-checks-grid.fs-5
              %p.fs-6 Catalog
        .mb-3
          - if user_signed_in?
            = link_to favorites_path, class: 'text-decoration-none text-dark rounded-circle' do
              .d-flex.align-items-center.gap-3
                %i.bi.bi-bookmark.fs-5
                %p Favorites
          -else
            = link_to new_user_session_path, class: 'text-decoration-none text-dark rounded-circle' do
              .d-flex.align-items-center.gap-3
                %i.bi.bi-bookmark.fs-5
                %p Favorites
        .mb-3
          - if user_signed_in?
            = link_to messages_path, class: 'text-decoration-none text-dark' do
              .d-flex.align-items-center.gap-3
                %i.bi.bi-bell.fs-5
                %p Messages
          - else
            = link_to new_user_session_path, class: 'text-decoration-none text-dark' do
              .d-flex.align-items-center.gap-3
                %i.bi.bi-bell.fs-5
                %p Messages
        .mb-3
          = link_to tops_path, class: 'text-decoration-none text-dark' do
            .d-flex.align-items-center.gap-3
              %i.bi.bi-arrow-up-short.fs-3
              %p.fs-6 Top
        .mb-3
          - if user_signed_in?
            %li
              %hr.solid
            = button_to 'Edit profile', edit_user_registration_path, method: :get, class: 'li-dropdown text-dark mt-3 mb-2'
            - if user_signed_in? && current_user.admin
              .mb-2.mt-2
                = button_to 'Create book', new_book_path, method: :get, class: 'li-dropdown text-dark mt-2 mb-3'
            %li
              %hr.solid
            .d-flex.justify-content-between
              = button_to 'Sign out', destroy_user_session_path, method: :delete, data: { turbo: false }, class: 'li-dropdown text-dark mt-2'



